  # 终端1 - 后端
  cd backend && .venv/Scripts/python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8002

  # 终端2 - 前端
  npm run dev

  打开 http://localhost:5173 →选择星盘/解梦 →填表提交 →等待约 20 秒即可看到 DeepSeek AI 分析报告。